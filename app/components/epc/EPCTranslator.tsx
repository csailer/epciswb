import React from 'react';
import { Theme, Input } from '@material-ui/core';

const EPCTranslator = () => {
  return <Input type="textbox" placeholder="EPC or GS1 Barcode" />;
};

export default EPCTranslator;
